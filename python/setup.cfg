[metadata]
# Version + project metadata live in pyproject.toml. This file
# exists so tooling that still wants a setup.cfg (old pip, old
# setuptools, or editable-install compatibility) finds something.
name = shrike-py

[options]
python_requires = >=3.8

[options.packages.find]
include = shrike*
